export function products(state) {
  return state.products;                    // For Product List Page
}

export function product(state) {
  return state.product;                     // For Product Detail Page
}

export function cart(state) {
  return state.cart;                         // For Cart Functionallity
}
